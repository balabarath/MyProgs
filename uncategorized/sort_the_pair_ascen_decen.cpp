#include<stdio.h>
#include<iostream>
#include<vector>
using namespace std;
int main()
{
    int t,a,b,i,j,k,n,max;
    vector< pair<int,int> > v;
    scanf("%d",&t);
    while(t--)
    {
     scanf("%d %d",&a,&b);
     
    }
    return 0;
}
