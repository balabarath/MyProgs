#include<stdio.h>
int main()
{
    int t,n,i;
    scanf("%d",&t);
    while(t--)
    {
      scanf("%d",&n);
      char a[n];
      for(i=0;i<n;i++)
      if(a[i]=='1')
      {
       set_zero();
      }
    }
    return 0;
}
